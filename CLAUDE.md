# Claude Code Instructions

## Git Workflow
- After completing any task, merge changes directly into `main` and push.
- Do **not** leave changes only on a feature branch or ask the user to open/merge a PR manually.
- Typical flow: implement → commit on working branch → `git checkout main` → `git merge` → `git push origin main`.
